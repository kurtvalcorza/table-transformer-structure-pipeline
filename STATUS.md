# Release status

Current status: **Candidate** — tutorial source complete, local pre-flight executed, awaiting supported-runtime execution. The pipeline package with its adaptation contract (`evaluate_zero_shot`, `adapt` with the zero-shot / frozen / unfrozen policies and validation selection, `evaluate`, `predict_objects`, `recognize_adapted`, `save_artifact` / `from_artifact`; `recognize` unchanged; the 94 embedded public-domain SciTSR-PD tables with derived structure boxes in `sample_data.py` and the dataset contract and BYOD loaders in `samples.py`; per-label AP@0.5 / AP@0.75 / AP, grid agreement, the operating points and the grid prior in `metrics.py`), offline unit tests (32, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (grid prior 38.7 % → zero-shot 88.7 % → restricted heads 85.2 % → selected policy `unfrozen last 2 decoder layers + restricted heads` 86.8 % mAP@0.5 on 21 held-out tables; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/table_transformer_structure_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's local fresh-kernel and Kaggle CPU runs are retained as history in `docs/release-verification.md` and are not evidence for the `E2E` blob. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
